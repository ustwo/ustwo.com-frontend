machine:
  services:
    - docker

deployment:
  dockerhub:
    branch: master
    commands:
      - $TRIGGER_DOCKER_HUB_BUILD

dependencies:
  override:
    - make compiler-build build
    - make assets-create app-create
test:
  override:
    - make test
