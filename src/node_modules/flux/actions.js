import Store from 'flux/store';

const Actions = {
  goTo(pageId) {
    Store.setPage(pageId);
    Store.emitChange();
  },
  toggleContacts() {
    Store.toggleContacts();
    Store.emitChange();
  }
};

window.Actions = Actions;

export default Actions;
