import BlogCategoryTag from './';

const Sandbox = React.createClass({
  render() {
    return <div className="sandbox full-width-component">
      <BlogCategoryTag category="Category" />
    </div>;
  }
});

export default Sandbox;
